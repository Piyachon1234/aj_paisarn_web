import React from 'react';
import { Container } from 'react-bootstrap'; // Import Container from react-bootstrap
import Button from 'react-bootstrap/Button';

const Home = () => {
    return (
        <Container> 
                <h1 className='topic'>Home</h1>
        </Container>
    );
};

export default Home;
